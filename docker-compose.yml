version: "3.8"

services:
  ollama:
    build: ./ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama
    command: ["sleep", "infinity"]

  agent:
    build: ./agent
    ports:
      - "8000:8000"
    depends_on:
      - ollama

volumes:
  ollama-data:
